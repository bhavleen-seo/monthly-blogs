.env
node_modules/
.next/
tsconfig.tsbuildinfo
data/blog-agent.json

# CS Publisher plugin — per-client copies contain real secrets so they stay local only.
# The .example.php and -template.php files are committed (no secrets).
wp-plugin/cs-publisher.php
wp-plugin/cs-publisher-*.php
!wp-plugin/cs-publisher-template.php
